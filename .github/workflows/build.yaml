name: Build

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2

      - name: Setup dependencies
        run: |
          sudo apt update
          sudo apt install -y protobuf-compiler
          corepack enable pnpm

      - name: Setup Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # 6.3.0
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: Setup Rust toolchain
        run: rustup toolchain install stable --profile minimal

      - name: Install etcdctl
        env:
          ETCD_VER: v3.6.11
        run: |
          curl -fsSL "https://github.com/etcd-io/etcd/releases/download/${ETCD_VER}/etcd-${ETCD_VER}-linux-amd64.tar.gz" -o /tmp/etcd-${ETCD_VER}-linux-amd64.tar.gz
          tar xzf /tmp/etcd-${ETCD_VER}-linux-amd64.tar.gz -C /tmp
          sudo install /tmp/etcd-${ETCD_VER}-linux-amd64/etcdctl /usr/local/bin/etcdctl
          etcdctl version

      - name: Setup environment
        run: sudo docker compose -f ci/docker-compose.yaml up -d

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 #2.9.1

      - name: Cache UI build
        id: ui-cache
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # 5.0.4
        with:
          path: crates/aisix-admin/ui-dist
          key: ui-dist-${{ hashFiles('crates/aisix-admin/ui/src/**',
            'crates/aisix-admin/ui/index.html',
            'crates/aisix-admin/ui/vite.config.ts',
            'crates/aisix-admin/ui/tsconfig*.json',
            'crates/aisix-admin/ui/package.json',
            'crates/aisix-admin/ui/pnpm-lock.yaml', 'crates/aisix-admin/build.rs',
            'crates/aisix-admin/Cargo.toml') }}

      - name: Build UI
        if: steps.ui-cache.outputs.cache-hit != 'true'
        run: cargo build -p aisix-admin --features build-ui -vv

      - name: Run Lint
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Build
        run: cargo build --locked

      - name: Run Test
        run: cargo test --workspace --locked

      - name: Run E2E Test
        run: |
          pnpm -C tests install --frozen-lockfile
          pnpm -C tests test

      - name: Upload artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: binary-debug
          path: target/debug/aisix
